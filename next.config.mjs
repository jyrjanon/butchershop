// next.config.js
/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // You can remove the 'images' block below if you are only using local images
  // images: {
  //   domains: ['images.unsplash.com', 'i.postimg.cc'],
  // },
};

module.exports = nextConfig;